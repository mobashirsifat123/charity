IRWA WEBSITE SUMMARY

What it is:
IRWA is a charity and community website. People can donate to causes, read articles, read fatwas, ask questions, and join the newsletter.

How it developed:
It started as a simple charity crowdfunding site called ChariFund.
Later, it added better admin tools, online payments, and a stronger database/storage setup.
After that, it grew into IRWA, which now also includes articles, fatwas, bilingual support, and content management tools.
Current improvement work is focused on UX and speed by removing unnecessary waiting, reducing heavy front-end work, and making pages feel faster.
The platform is now also being restructured into a broader Dawah and Quran-learning product, with early work started on scholar roles, Quran progress tracking, and global audio playback.
The charity tools are expanding too, including a live-price Zakat calculator that can send users directly into Stripe checkout as a dedicated zakat payment flow.
Daily retention tools are being added as well, including a local prayer-times widget with browser geolocation, fallback city timings, countdown to the next prayer, and a simple Qibla compass.
The Dawah section is also expanding into structured learning, with early LMS foundations for scholar-led courses, video modules, classroom pages, and saved user progress.
The charity side is also becoming more transparent, with live donation pop-up notifications and a new impact dashboard showing totals, campaign progress, category breakdowns, and charts.
The admin panel is being reorganized too, with clearer sidebar groups for charity, dawah, learning, and community work, plus role-based access so scholars only see the parts they are meant to manage.
The Dawah admin tools are also getting richer, with new forms for scholar profiles, article creation, Supabase image uploads, and a richer article editor so content can be managed without changing code.
The learning admin side is expanding as well, with a real course builder that lets admins create course overviews, upload course covers, and keep adding, editing, or removing video modules over time.
The visual experience is also being polished with reusable micro-interactions like smoother card hover lift, soft color glow, and tactile button press effects to make the site feel more premium.

Latest bug-fix update (March 30, 2026):
- Fixed major build breaks on the Impact dashboard caused by database column differences.
- Added safer fallback handling so pages still load even when some newer database tables are not ready yet.
- Improved Next.js 15 route safety by fixing dynamic route parameter handling in admin APIs and editor pages.
- Fixed live Zakat metal-price parsing so real API data is accepted correctly.
- Cleaned remaining lint warnings and verified that both development and production builds now run successfully.

Frontend QA stabilization update (March 30, 2026):
- Ran a broad browser-based frontend route sweep (public pages + admin pages) and checked runtime console errors.
- Fixed homepage hydration mismatch in the Prayer Times countdown logic.
- Fixed featured campaigns fetch errors by making homepage campaign reads schema-safe for older databases.
- Added working favicon handling to remove missing icon frontend errors.
- Fixed dynamic content page edge-cases so invalid article/fatwa URLs no longer trigger UUID query errors in the browser console.
- Added the recommended Next.js `data-scroll-behavior=\"smooth\"` attribute to remove navigation warning noise.
- Re-verified with lint, production build, and repeated route checks after patching.

Backend access and stability update (March 30, 2026):
- Fixed local backend reachability by moving the legacy API default from port 5000 to 5050 (5000 was occupied by macOS AirPlay/AirTunes on this machine).
- Updated frontend API defaults and env examples to point to the corrected backend port.
- Added backend startup scripts (`start` and `dev`) in the legacy Express package for predictable local runs.
- Hardened backend environment loading so Express reads both backend `.env` and root `.env.local`/`.env`.
- Added graceful database configuration handling, so DB-related routes return clear `503` messages instead of opaque `500` crashes when DB env vars are missing.
- Fixed auth middleware behavior so protected routes correctly return `401` when no token is provided.
- Updated campaign/donation/stripe controller ID handling to avoid numeric-only assumptions and better tolerate UUID-style identifiers.
- Verified backend route responses and CORS behavior from `http://localhost:3000` after patching.

Tech used:
- Next.js and React for the website
- Supabase for login, database, and image/file storage
- Stripe for payments
- Bootstrap and Sass for the design
- Express and PostgreSQL for older backend parts still kept in the repo

Main features:
- Donation campaigns with progress tracking
- Secure online donations
- Articles and blog-style content
- Fatwas and question submission
- Search and filtering
- Newsletter signup
- English and Arabic language support
- User dashboard for donation history and saved items
- Admin dashboard to manage content, donations, images, team members, and website settings

Deep diagnosis and bug-fix update (April 8, 2026):
- Ran a project-wide diagnosis across frontend routes, runtime fetches, auth/session boot flow, and backend startup.
- Fixed campaign detail runtime query behavior by switching strict single-row reads to safe optional reads, so missing campaigns no longer produce Supabase 406 browser errors.
- Applied the same safe optional campaign lookup in the donation flow, so invalid `?campaign=` links fail gracefully instead of surfacing noisy runtime errors.
- Stabilized auth bootstrap by changing strict `users` profile lookups to optional reads, reducing startup failures when a profile row is missing.
- Confirmed Next.js frontend still serves correctly on `http://127.0.0.1:3000` after these fixes.
- Isolated a backend environment blocker: Express dependency loading is hanging before `app.listen()` in this machine runtime, so legacy backend routes cannot reliably bind to port `5050` until runtime/dependency state is normalized.

Admin dynamic-control expansion (April 8, 2026):
- Replaced placeholder admin pages with live dynamic management screens for `Article Categories` and `Scholar Profiles` (list + create/edit/delete flow).
- Extended admin resource API mapping to support `article-categories` and `scholars` resources directly from the same secured admin endpoints.
- Added dashboard-driven custom navigation controls:
  - `nav_custom_links_json` for extra header links
  - `footer_quick_links_json` for extra footer quick links
  - `footer_causes_json` for fully configurable footer cause categories
- Updated frontend header/footer rendering to consume those settings safely, so future link/category updates can be done from Admin Site Builder without code changes.

Production stability hardening (April 8, 2026):
- Root instability fixed by moving runtime from experimental Node 25 to Node 22 LTS behavior and pinning project compatibility (`.nvmrc` + `package.json` engines).
- Fixed TypeScript/build deadlock by narrowing `tsconfig.json` include/exclude scope and removing stray duplicate dependency directories that caused endless scanning.
- Rebuilt frontend and backend dependencies cleanly under Node 22 and verified backend starts reliably on `http://localhost:5050`.
- Added resilient fallback handling for the Impact dashboard data loader so production builds no longer fail when external Supabase/network requests are temporarily unavailable.
- Removed Sass import deprecation warning by migrating from `@import` to `@use` for micro-interaction stylesheet wiring.
- Re-verified core health routes and admin pages after fixes (`/api/ping`, `/admin/dashboard`, `/admin/categories`, `/admin/scholars`).

Final pre-launch QA pass (April 9, 2026):
- Ran full lint + production build under Node 22 LTS and confirmed both pass consistently.
- Executed production-runtime smoke checks for public pages, core admin pages, and key APIs.
- Fixed newsletter API validation bug so invalid emails now return proper `400 Bad Request` instead of `500`.
- Confirmed frontend production server route health (`/`, `/impact`, `/courses`, `/admin/dashboard`, `/admin/categories`, `/admin/scholars`, `/api/ping`).
- Confirmed backend process stability on `http://localhost:5050`.
- Backend DB-dependent routes still return explicit `503` until deployment environment sets a valid database connection (`DATABASE_URL` or `DB_*` vars).

Fatwa & Article redesign update (April 9, 2026):
- Rebuilt `/blog-grid` and `/fatwa` into an Islamweb-inspired directory layout (content-first list feed instead of large cards).
- Added left sidebar navigation blocks on both pages with:
  - search input
  - “By Subject” filters
  - “By Author” filters
  - “New Articles” / “New Fatwas” quick lists
- Updated main feed items to match “title + excerpt + compact metadata + More link” flow.
- Added shared styling utilities in global stylesheet for this new layout (`.islamweb-like-*` classes).
- Re-ran lint and production build after redesign to confirm no compile regressions.

Dark green premium theme redesign update (April 9, 2026):
- Replaced the old mixed orange/blue palette with a single green-and-white design system built around:
  - deep green primary actions
  - rich green secondary surfaces
  - soft gold highlights used sparingly
  - white and light green-tinted backgrounds for cleaner reading sections
- Reworked the global stylesheet so buttons, cards, forms, pagination, header, footer, badges, dropdowns, and article/fatwa directory blocks now follow the same color tokens and spacing system.
- Restyled the most visible homepage and shared sections to match the new theme, including:
  - hero/banner
  - article highlights
  - fatwa highlights
  - community section
  - CTA section
  - featured campaigns
  - prayer times & Qibla widget
  - impact charts
  - breadcrumb headers
- Updated the admin shell to visually match the public site better, including the sidebar and dashboard wrapper background treatment.
- Updated the generated favicon to match the new green and gold identity.
- Verified the redesign with lint, production build, and route smoke checks for `/`, `/blog-grid`, `/fatwa`, and `/admin/dashboard`.

Full-site premium UX extension (April 9, 2026):
- Extended the green premium theme beyond the homepage so more of the site now follows the same design language:
  - blog detail pages
  - fatwa detail pages
  - campaign listings and donation modal
  - impact dashboard
  - zakat calculator
  - courses catalog, course overview, and classroom pages
  - donor dashboard and donation success flow
  - testimonials, partners, team, and “how it works” sections
- Added stronger global Bootstrap-style overrides so alerts, badges, secondary buttons, inputs, pills, tables, list groups, and utility surfaces now feel consistent across public pages and admin pages too.
- Improved the reading experience with cleaner hero sections, calmer backgrounds, richer content panels, and more consistent share/action controls.
- Improved tactile feel with wider use of premium hover-lift and ripple interaction styling on calls to action.
- Re-ran lint, production build, and a broader route smoke test covering `/`, `/blog-grid`, `/fatwa`, `/impact`, `/courses`, `/zakat`, `/dashboard`, and `/admin/dashboard`.

Authentication upgrade (April 9, 2026):
- Fixed the broken “Forgot Password?” flow by replacing the old dashboard redirect with a real reset-password journey.
- Added a dedicated `/reset-password` page where users can open the email link and set a new password safely.
- Added a dedicated `/auth/callback` page to complete Supabase OAuth redirects cleanly in Next.js 15.
- Added “Continue with Google” on both login and registration pages so users can sign in with their existing Google account session in the browser instead of manually creating a password-first account.
- Improved auth profile hydration so Google-based sign-ins can also create/sync a matching `public.users` record when needed.
- Re-ran lint and production build after the auth changes to confirm the new routes compile and deploy safely.

E-Book library launch (April 9, 2026):
- Added a new public `/ebooks` section inspired by the Islamweb ebook directory structure.
- Built the new ebook listing page with:
  - search
  - category filtering
  - sticky sidebar navigation
  - “New E-Books” list
  - paginated directory-style feed
- Added a dedicated ebook detail page at `/ebooks/[slug]` with:
  - premium book cover treatment
  - metadata summary
  - ebook description
  - related ebooks sidebar
- Added ebook navigation links into the main header and footer so the section is part of the primary site structure.
- Implemented the first structured ebook content dataset as a reusable library source file so the section can be expanded cleanly later.
- Re-ran lint, production build, and direct route checks for `/ebooks` and `/ebooks/foundations-of-faith`.

Futuristic visual refinement (April 9, 2026):
- Took selective inspiration from the visual language of Figma Weave without copying its full sci-fi style.
- Added a softer “future-forward” layer to IRWA’s homepage by introducing:
  - subtle grid/aurora background treatment
  - premium glass-like surfaces
  - luminous system/status chips
  - a more structured “platform workflow” hero panel
- Kept the implementation within IRWA’s calm green-and-gold identity so the site still feels trustworthy and suitable for Islamic education and charity work.
- Re-ran lint and production build after the visual upgrade to confirm the new styling is safe.

Shared modular design expansion (April 9, 2026):
- Extended the Weave-inspired design language beyond the homepage hero so the site feels more intentionally “designed” across multiple sections.
- Added reusable UI primitives to the global stylesheet, including:
  - section header rails
  - light glass panels
  - modular information lists
  - structured data chips
  - trust/partner cloud cards
- Applied the refined design system to:
  - the homepage article highlights section
  - the homepage fatwa highlights section
  - the prayer times and Qibla widget
  - the partners/trust band
  - the donor testimonial section
- Preserved the core IRWA palette with elegant dark green, white, and restrained gold so the platform stays calm and premium rather than overly futuristic.
- Re-ran lint and production build after the shared design rollout to confirm the expanded styling remains production-safe.

Global color-system sweep (April 9, 2026):
- Performed another pass specifically to remove visual drift and reinforce the green-and-white design system across more sections.
- Added more reusable theme utilities for:
  - soft green badges
  - gold highlight badges
  - icon tints
  - admin metric cards
  - admin surfaces
  - sidebar shells and active states
- Reworked additional sections so they align more tightly with the approved palette:
  - featured causes / campaigns
  - community impact section
  - “how you can help” cards
  - impact / difference section
  - admin sidebar
  - admin dashboard summary cards and panels
- Reduced the feeling of isolated one-off colors by leaning harder on the shared green, white, and subtle gold visual language.
- Re-ran lint and production build after the color consistency pass to confirm the design cleanup is safe for production.

Deployment hardening pass (April 9, 2026):
- Ran a deeper production-readiness sweep focused on build stability, runtime health, API behavior, and deployment configuration.
- Confirmed the Next.js app builds successfully in production mode and that key routes respond correctly in a running server environment.
- Confirmed the legacy backend boots successfully and now clearly reports `503` when database configuration is missing instead of failing silently.
- Added server-side environment guards so critical routes fail more clearly when live configuration is incomplete:

Admin article upload resilience update (April 23, 2026):
- Fixed the admin article create flow so an article can still be saved even if the Supabase storage bucket for cover images is missing or temporarily misconfigured.
- Added clearer admin feedback so image-upload problems are shown as warnings instead of blocking the full article submission.
- Relaxed the article save path to fall back to the base blog schema when advanced article columns are not yet available in Supabase, preventing another dead-end failure during creation.

Language stability update (April 24, 2026):
- Made the selected language stable for the browser session by storing the manual choice in session storage and reusing it across page navigation.
- Removed browser language auto-detection from the active language flow so the site does not unexpectedly switch languages after the user chooses English, Arabic, or Bengali.
- Delayed Bengali auto-translation until the stored language choice has loaded, preventing mixed Bengali and English text caused by early translation during page startup.
- Tightened Bengali auto-translation further by caching translated text in the browser, reapplying known translations immediately after React updates, and preventing the translator from reacting to its own DOM changes.
- Replaced the continuous Bengali DOM watcher with a controlled scheduled translation flow, so Bengali mode no longer constantly fights React renders and flips text between English and Bengali.

Admin dashboard schema fallback update (April 24, 2026):
- Fixed the admin dashboard API so it no longer crashes when the live Supabase donations table does not include the newer `donor_name` column.
- Added a fallback donation query that uses available donation fields and keeps the recent-donations table working.
- Updated the admin dashboard to show a masked donor email or Anonymous when a donor name is unavailable.

Header donation shortcut cleanup (April 24, 2026):
- Removed the always-visible Donate Now shortcut from the desktop header and mobile menu.
- Kept donation access inside the proper campaign/support sections lower on the page so giving remains available without disturbing the main interface.

Bengali language stability hardening (April 24, 2026):
- Removed the live Bengali DOM auto-translator because it was rewriting rendered text after React updates and causing visible language flicker/vibration.
- Kept the stable language preference system and built-in Bengali UI translations so choosing Bengali no longer makes the page continually switch between Bengali and English.

Qibla compass animation update (April 24, 2026):
- Replaced the old hand-built SVG Qibla compass with the supplied animated Lottie compass artwork.
- Kept the existing Qibla direction math and device-heading support so the new animation rotates toward the Qibla when location/heading data is available.
- Added the Lottie player dependency and stored the compass animation as a local app asset.
- Disabled endless Lottie playback so the compass stays stable when the phone is stable and only rotates from real Qibla/heading data.
- Smoothed live compass movement by ignoring tiny device-heading jitter and easing accepted heading changes more gradually.

Location consent logging update (April 24, 2026):
- Updated the prayer times and Qibla location flow so every successful user-approved location lookup is sent to the Supabase `location_consents` log.
- Authenticated members are linked to the saved location log through their Supabase auth user and profile/email when available.
- The saved GPS coordinates continue to power local prayer times and Qibla direction after the user explicitly allows location access.

Mobile UX optimization pass (April 24, 2026):
- Removed the floating login/register quick-access bar that stayed on screen while scrolling on mobile.
- Tightened the mobile header, homepage hero, language switcher, search bar, CTA buttons, quick cards, article subject cards, and major content panels for better phone readability.
- Reduced mobile visual clutter so the first screen shows the core learning/search actions more clearly.
  - newsletter subscriptions
  - Stripe checkout session creation
  - Stripe donation verification
  - Stripe webhooks
- Improved production branding and deployment readiness by updating:
  - root metadata/title/description
  - deployment environment example file
  - README deployment checklist and required environment variables
  - default live contact values in site settings
- Corrected deployment-related config details including:
  - Next image upload port alignment with the backend on `5050`
  - Supabase storage hostname typo in `next.config.js`
- Reduced noisy fallback logging from intended course/impact fallback behavior so production build logs stay cleaner.
- Re-ran lint and production build after the hardening pass to confirm the deploy-facing fixes remain safe.

IRWAA logo rollout and auth UI refinement (April 9, 2026):
- Added the new IRWAA logo asset to the app and made it the default shared site logo.
- Updated the shared site settings so the logo now appears across the main brand surfaces without needing per-page hardcoding.
- Refined logo usage in:
  - desktop header
  - mobile header
  - footer
  - login page
  - registration page
- Added a subtle site-wide background watermark using the IRWAA logo so the brand is visible across the experience without overwhelming readability.
- Improved the header auth actions so the login and register buttons feel smaller, cleaner, and more balanced with the navigation.
- Restyled the login and registration screens with stronger branding, calmer spacing, and more polished call-to-action buttons.
- Replaced raw auth-page logo `<img>` elements with optimized Next.js `Image` usage to keep the implementation cleaner and lint-safe.
- Re-ran lint and production build after the branding pass to confirm the new logo integration and auth polish are production-safe.

Bengali language support and auto-translation layer (April 9, 2026):
- Expanded the shared language system from English/Arabic to English/Arabic/Bengali.
- Added Bengali to the language switcher so visitors can explicitly switch the site into Bangla.
- Updated locale detection so browsers already configured for Bengali can default into the Bengali experience.
- Added a lightweight site-wide Bengali auto-translation layer that translates rendered page content after Bengali is selected.
- Added a translation API route to support Bengali translation requests and cache repeated translations.
- Kept English and Arabic behavior unchanged, while ensuring the Bengali translator restores original content when a visitor switches back.
- Re-ran lint and production build after the language upgrade to confirm the new locale and translation layer are production-safe.

About page team-section expansion (April 9, 2026):
- Added the same “Meet the Changemakers” team section to the About Us page so visitors can discover the organization story and the team in one place.
- Reused the existing dynamic `TeamOne` component instead of duplicating content, so the About page stays synced automatically with homepage team updates.
- Kept the section fully admin-managed:
  - team members remain editable from the Team admin area
  - team badge/title/description remain editable from site content/settings
- Re-ran lint and production build after the About page update to confirm the new section is production-safe.

Hero-only language selection behavior (April 9, 2026):
- Moved the visible language switcher out of the header and mobile menu so visitors are no longer prompted to change language on every page.
- Added the shared language switcher to the homepage hero as the primary place to manually choose English, Arabic, or Bengali.
- Kept the selected language global across the full site experience, so once a visitor chooses a language it continues on other pages until they manually change it again.
- Styled the hero language switcher to match the premium green-and-white design system without disrupting hero readability.
- Re-ran lint and production build after the language-control update to confirm the new behavior is production-safe.

Islamweb-inspired article hub redesign (April 9, 2026):
- Rebuilt the public articles page into a richer editorial directory inspired by the Islamweb articles experience while keeping the IRWA green-and-white design system.
- Added a multi-layer article layout including:
  - featured lead articles
  - sub-section category chips
  - selected section shelves
  - a denser latest-articles feed
  - an editorial sidebar with search, subjects, authors, and latest links
- Integrated article cover images into the featured strip and selected-section cards for a stronger editorial presentation.
- Kept the page fully admin-driven by wiring it to existing dynamic sources:
  - article records from the admin blog/article manager
  - article categories from the article categories manager
  - author/scholar metadata already attached to articles
  - new article-directory layout copy/settings from the site content builder
- Added new site-builder settings so admins can change the article directory title, description, section headings, selected categories, sidebar labels, and “more articles” link without touching code.
- Added article category fetching support to the shared content data helpers so the page can render managed sub-sections from the database.
- Re-ran lint and production build after the redesign to confirm the new article architecture is production-safe.

Selective background image preview for About & Contact (April 9, 2026):
- Added the provided desert image into the project as a dedicated background asset for visual testing on the About Us and Contact Us pages only.
- Wrapped the About and Contact content cards inside a themed photo section with a soft overlay so the background can be evaluated without sacrificing readability.
- Used a translucent surfaced card treatment over the photo instead of placing text directly on the image.
- Kept the experiment isolated to those two pages so the rest of the site’s cleaner green-and-white reading experience remains untouched.
- Re-ran lint and production build after the background preview update to confirm the change is production-safe.

Contact section merged into About page (April 9, 2026):
- Removed the standalone contact experience from primary navigation and merged the contact content directly into the About Us page as an anchored section underneath the main About content.
- Added a dedicated `#contact-us` section inside the About page so visitors can read about the organization first and then reach contact details in the same flow.
- Updated shared navigation and call-to-action links so “Contact Us” now points to `/about-us#contact-us`.
- Kept the old `/contact-us` route as a redirect to the About page contact anchor so existing links still work.
- Re-ran lint and production build after the merge to confirm the new structure is production-safe.

Search navigation cleanup and page-level filters (April 9, 2026):
- Removed the dedicated Search link from the main site navigation so the header feels cleaner and less crowded.
- Removed the global header search trigger so search is no longer competing with the primary content navigation.
- Moved search behavior into compact, page-level toolbars on the Fatwas and E-Books pages where it is more contextually useful.
- Kept those new search/filter controls intentionally compact so they help discovery without taking over the page layout.
- Left the sidebar browsing structures intact on Fatwas and E-Books, but removed the larger duplicated sidebar search blocks to save space.
- Re-ran lint and production build after the search UX cleanup to confirm the new navigation and filter layout are production-safe.

Homepage Quran learning conversion (April 9, 2026):
- Replaced the old featured “Causes” homepage block with a dedicated Quran learning showcase.
- Updated the homepage hero’s fourth quick-access card from “Causes” to “Learn Quran”.
- Changed the hero support link fallback so it now points toward Quran learning instead of the causes section.
- Added a new homepage Quran-learning component with room for multiple short video lessons presented as a premium learning grid.
- Kept the new section admin-manageable through site builder settings, including:
  - Quran learning badge
  - section title
  - section description
  - CTA text/link
  - a JSON list of short video lesson cards
- Added new shared styling for the Quran learning showcase so it matches the green-and-white premium design system.
- Re-ran lint and production build after the homepage conversion to confirm the new section is production-safe.

IRWAA wordmark visibility fix (April 9, 2026):
- Fixed the shared brand treatment so the visible wordmark is consistently shown as `IRWAA` alongside the small logo instead of depending on older saved site-name values.
- Updated the header, mobile menu, footer, login page, and registration page to use the logo-plus-wordmark presentation.
- Adjusted the shared brand layout styles so the wordmark stays visible, aligned, and non-wrapping across common screen sizes.
- Updated the default site settings brand name to `IRWAA` for better consistency with the intended identity.
- Re-ran lint and production build after the wordmark fix to confirm the branding update is production-safe.

Mobile-first UX pass for key public pages (April 10, 2026):
- Performed a focused mobile usability pass on the homepage, header, articles page, fatwas page, and ebooks page.
- Improved the homepage first screen on mobile by:
  - reducing hero height and vertical padding
  - hiding the heavier desktop workflow panel on smaller screens
  - adding a clear 2x2 quick-access grid for primary destinations
  - making hero CTAs full-width and easier to tap
  - tightening hero stat and language-switch layouts
- Improved article browsing on mobile by:
  - adding a compact top search/filter toolbar for smaller screens
  - hiding the large desktop sidebar on mobile
  - tightening typography, spacing, and card sizing for easier reading
- Improved fatwa and ebook browsing on mobile by:
  - hiding desktop-heavy sidebars on smaller screens
  - keeping compact filter bars near the top of content
  - preserving faster access to the main content list without extra scrolling
- Added shared responsive utility styling to support mobile chips, compact filter bars, tighter section spacing, and more usable tap targets.
- Re-ran lint and production build after the mobile-first pass to confirm the UX updates are production-safe.

Technical SEO foundation pass (April 10, 2026):
- Strengthened the global site metadata with:
  - better default title/template handling
  - richer description and keyword coverage
  - canonical homepage definition
  - Open Graph metadata
  - Twitter card metadata
  - crawler directives for richer previews
- Added structured data in the root layout for:
  - Organization schema
  - WebSite schema with search action support
- Added core technical SEO routes:
  - `/robots.txt`
  - `/sitemap.xml`
  - `/manifest.webmanifest`
- Added route-level metadata layouts for key public index pages:
  - About
  - Articles
  - Fatwas
  - E-Books
- Added `noindex` metadata for internal/utility routes that should not rank in search results, including:
  - Search
  - Login
  - Register
  - Reset Password
  - Request Fatwa
- Kept search engine focus on the stronger public-facing knowledge pages while reducing index noise from low-value utility screens.
- Re-ran lint and production build after the SEO pass to confirm the new metadata and crawl files are production-safe.

Dynamic detail-page SEO pass (April 10, 2026):
- Added server-rendered SEO metadata for individual article pages so each article now exposes:
  - a dynamic title
  - a dynamic description
  - canonical URL data
  - Open Graph and Twitter preview data
- Added server-rendered SEO metadata for individual fatwa pages so each fatwa now exposes:
  - a dynamic title
  - a dynamic description
  - canonical URL data
  - Open Graph and Twitter preview data
- Added structured data for detail pages:
  - `Article` schema for article pages
  - `QAPage` schema for fatwa pages
- Removed the old client-only document title/meta mutation from detail pages so SEO no longer depends on browser-side hydration.
- Kept the implementation aligned with Next.js 15 route-level metadata and existing Supabase content fetching.

Auth stability and Supabase compatibility pass (April 10, 2026):
- Hardened the browser Supabase client with explicit auth session persistence, token refresh, URL session detection, and PKCE flow settings.
- Removed the brittle direct `public.users` insert from browser-side registration so account creation no longer depends on client-side table write permissions.
- Improved auth hydration so login can still succeed even if reading the custom `public.users` profile is blocked or temporarily unavailable.
- Preserved user display data by falling back to Supabase Auth user metadata when the custom profile table cannot be read.
- Improved Google sign-in/sign-up UX by removing forced account re-selection so browser-signed Google accounts can flow more naturally.
- Fixed auth callback handling to support both PKCE `code` redirects and token/hash-based redirects.
- Fixed password reset handling to support both PKCE `code` redirects and token/hash-based recovery links.

Database integration hardening pass (April 10, 2026):
- Fixed the course progress server action to store Supabase module UUIDs correctly instead of coercing them into numbers.
- Fixed course creation and module creation/update/delete flows to keep Supabase UUID identifiers intact for:
  - `courses.id`
  - `course_modules.id`
  - `course_modules.course_id`
  - `courses.scholar_id`
- Added safer admin course-data fallbacks when Supabase server environment variables are missing or placeholder values are still present.
- Reduced the chance of silent LMS/database failures caused by UUID-to-number conversion mismatches.

Design, stability, and deployment hardening pass (April 17, 2026):
- Upgraded the article experience with a stronger editorial directory, richer article cards, improved related-content visuals, and a calmer long-form reading layout.
- Added a shared read-time helper so homepage article cards, article shelves, latest-feed items, related cards, and article detail pages show consistent reading-time metadata.
- Improved shared readability styling with better typography, spacing, focus states, form controls, and mobile behavior for reading-heavy pages.
- Fixed a broken article-feed layout where feed text could escape the intended grid structure because the feed body wrapper was missing.
- Fixed the homepage article section so it fetches enough articles to properly fill the new lead-plus-supporting-story layout.
- Fixed the blog directory for Next.js 15 static generation by wrapping `useSearchParams()` usage behind a Suspense boundary.
- Fixed build-time deployment crashes caused by:
  - a missing `article-subjects` dependency file in the tracked repo
  - a broken ESLint 9 flat-config setup
  - a stray duplicate `node_modules/@types/node 2` folder causing TypeScript to resolve an invalid `node 2` type library
  - admin/newsletter/fatwa-request API routes initializing Supabase too early during build instead of at request time
- Re-ran real repo verification in the Git-tracked project:
  - `npm run lint` now completes without errors (warnings remain in older admin/search files)
  - `npm run build` now completes successfully

Final push cleanup and auth/member portal stability pass (April 20, 2026):
- Synced the latest member-auth and article-admin updates into the GitHub-tracked repo before deployment.
- Preserved the Google OAuth callback/session exchange flow so signed-in users can land in a real member session and see the member dashboard entry.
- Kept the server-backed authenticated profile endpoint for loading/creating member profiles and preserving admin access for configured admin emails.
- Ensured the header member avatar remains visible on desktop and mobile, with an admin-only crown/admin-panel link for admin accounts.
- Removed accidental duplicate copied route files before pushing so Vercel does not deploy junk app routes.
- Cleaned remaining lint warnings in older admin/search/settings/favicon files.
- Verified the deployable repo with:
  - `npm run lint`
  - `npm run build`

Member dashboard access fix (April 20, 2026):
- Changed the signed-in Google/member avatar in the header so clicking the picture goes directly to `/dashboard`.
- Kept a small separate chevron beside the avatar for quick account-menu actions such as logout and admin shortcuts.
- Added the Google/member profile photo into the dashboard hero so each member can immediately recognize their account.
- Added an admin crown badge and prominent Admin Panel button inside the dashboard for admin users only.
- Fixed a dashboard prerender bug where the page could read `user.avatar_url` before auth hydration completed.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Bengali-first language and homepage copy pass (April 20, 2026):
- Made Bengali the default visitor language while still preserving manual English/Arabic/Bengali switching.
- Added Bengali-localized homepage hero defaults so the hero opens with stronger faith-centered Bangla copy.
- Replaced the old generic hero line with a stronger message about Quran learning, trusted answers, meaningful causes, and faith becoming light/action.
- Removed the generic “Featured Mission” prefix from the hero badge so it now works as a clean intro line.
- Improved the Bengali auto-translation layer so it also responds to async text updates inside existing DOM nodes, not only newly inserted elements.
- Added safeguards to avoid re-translating text that is already Bengali.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Qibla compass visual movement fix (April 20, 2026):
- Separated the Qibla pointer from the rotating compass face so the green Qibla marker visibly rotates according to the calculated direction.
- Kept live device-heading support where available, but made the fallback North-up mode show the Qibla angle with a clearly animated green needle.
- Added a Kaaba marker, green needle line, and subtle pulse animation to make the direction easier to understand on mobile.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Admin role safety correction (April 20, 2026):
- Removed hardcoded email-based admin promotion from the authenticated profile API.
- New member profiles are now created as normal `donor` users only.
- Admin visibility now depends only on the `public.users.role = 'admin'` value stored in Supabase.
- This prevents normal logged-in members from seeing the Admin Panel link unless their database role is explicitly admin.

Approved-admin allowlist correction (April 20, 2026):
- Added a strict approved-admin email allowlist for the two intended admin accounts only.
- The member profile API now promotes only approved admin emails to `admin` and forces all other users to `donor`, even if old database rows were accidentally marked admin.
- Server-side admin API protection now checks the approved-admin email allowlist before allowing admin access.
- Client-side auth hydration also prevents non-approved users from seeing admin UI if an old browser/database fallback reports an incorrect admin role.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Admin display fallback correction (April 20, 2026):
- Fixed client-side auth hydration so approved admin emails show as `admin` even if the profile API/database profile response is briefly stale.
- Normal users remain forced to `donor` because the approved-admin email allowlist still gates all admin UI and server access.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Admin content save `password_hash` fix (April 20, 2026):
- Fixed the Admin Site Builder save error caused by trying to insert Google OAuth users into the legacy `public.users` table without a required `password_hash`.
- Admin/profile helpers now return safe synthetic OAuth profiles when no legacy user row exists, instead of inserting incomplete `users` rows.
- Existing user rows can still have roles corrected safely, but new OAuth-only admins are no longer blocked from editing site settings.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Article subject hero upgrade (April 20, 2026):
- Moved the four core article subjects into the top green article-page hero area.
- Each subject now appears as a logo/icon card linking directly to its filtered Articles view.
- Removed the duplicate lower white “Browse by Subject” card block so the subject entry point is concentrated at the top.
- Added Site Builder fields for editing all four article subject titles, descriptions, and Font Awesome icon classes from the admin panel.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Article product hub upgrade (April 20, 2026):
- Expanded the Articles page into a richer learning hub with a reader-path panel, hero CTAs, quick filters, popular topic chips, and clearer empty states.
- The article list, images, categories, authors, tags, featured status, and body content remain dynamic from the admin article manager/Supabase.
- Added Site Builder fields so admins can edit the article-page hero CTAs, reader-path copy, quick-filter heading, topic-cloud heading, and empty-state copy without changing code.
- Kept the premium green-and-white visual system consistent across the new article modules, with mobile-first responsive styling.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Admin Server Action authorization fix (April 20, 2026):
- Fixed article, scholar, course, and module Server Actions so approved Google-admin accounts can save content even when they do not have a complete legacy `public.users` password row.
- Reused the approved-admin email fallback for Server Actions, matching the admin API behavior already used by the dashboard/resource routes.
- This prevents OAuth admins from entering the admin panel but then failing when creating article-related or LMS content.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Admin persistence and cross-device freshness fix (April 20, 2026):
- Removed stale browser `localStorage` caching for public site settings so another device no longer keeps old homepage/about/article copy after an admin save.
- Added a no-cache public site settings endpoint that reads the latest saved Supabase settings and is used by the global settings provider.
- Hardened the admin Site Builder save API so every Supabase write is checked; failed setting rows now return an error instead of a false success message.
- Added immediate public page revalidation after Site Builder changes, article/fatwa/campaign resource edits, and admin image uploads/deletions.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Dorar-inspired knowledge navigation and search upgrade (April 20, 2026):
- Reworked the public header away from generic template links such as Home, About Us, Causes, Pages, and Contact Us in the primary menu.
- Added a cleaner knowledge-first navigation focused on Quran, Articles, Fatwas, E-Books, Learn Quran, Courses, and Search.
- Added a reusable Dorar-style knowledge search bar with search-type selection and placed it prominently in the homepage hero, Articles page, Fatwa page, and E-Books page.
- Expanded the homepage hero feature grid into compact knowledge cards for articles, fatwas, Quran topics, courses, e-books, and search.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Dorar Hadith API-inspired homepage utilities (April 21, 2026):
- Added a Next.js `/api/hadith/search` proxy for Dorar-compatible Hadith API deployments using `DORAR_HADITH_API_BASE_URL`.
- Added safe fallback hadith content so the homepage remains useful even before the external Dorar proxy is hosted/configured.
- Added a homepage Hadith Explorer with quick topic icons, small encyclopedia-style feature cards, and a hadith preview showing narrator, scholar, book, grade, and categories.
- Added Site Builder fields for the Hadith Explorer badge, title, and description.
- Re-verified with:
  - `npm run lint`
  - `npm run build`

Admin Article/Fatwa persistence upgrade (April 21, 2026):
- Made Article and Fatwa admin saves strict so advanced fields no longer appear successful if Supabase is missing the required database columns.
- Added permanent admin-editable fields for article/fatwa directory summaries and category slugs used by the upgraded public Article and Fatwa sections.
- Fixed quick publish/draft status updates from the Article and Fatwa admin list pages.
- Expanded the Supabase content upgrade SQL to include summaries, primary category slugs, category slug arrays, indexes, and default article subject records.

Header and search UX cleanup (April 21, 2026):
- Replaced the slim top-bar text links with compact icon-only quick actions for Articles, Fatwas, Search, and social channels.
- Moved the homepage hero search bar higher so it is visible before the main CTA buttons.
- Removed duplicate secondary search inputs from the Article, Fatwa, and E-Book directory bodies while keeping the prominent top search bars functional for filtering.

Article admin CMS stability and subsection control (April 21, 2026):
- Replaced the fragile rich text editor with a stable admin-safe HTML editor so `/admin/blogs/new` no longer crashes before article entry.
- Added Article subsection selection to new/edit article forms so admins can place articles into public sections such as Aqidah, Fiqh, History, and Seerah.
- Expanded the Article Categories admin to manage subsection name, slug, description, icon class, sort order, and public visibility.
- Updated the public Article page to respect article `category_slugs` and dynamic category records instead of relying only on hard-coded subjects.

Runtime crash cleanup (April 21, 2026):
- Removed the incompatible `react-quill` dependency and global Quill stylesheet so production pages no longer load code that calls deprecated `findDOMNode`.
- Updated Sentry configuration to initialize only when a real DSN is provided, removing the invalid dummy DSN browser warning.

Bengali translation fallback fix (April 23, 2026):
- Hardened `/api/translate` so upstream translation failures no longer return a 500 error on the homepage.
- The Bengali auto-translator now limits each request batch and safely falls back to original text when translation is temporarily unavailable.

Article category and geolocation warning cleanup (April 23, 2026):
- Added a public `/api/article-categories` fallback endpoint so missing Supabase `article_categories` tables no longer create browser-visible 404 errors on the Article page.
- The Article page now falls back to built-in Aqidah, Fiqh, History, and Seerah category cards until the Supabase content schema is applied.
- Prayer time geolocation now remembers blocked/denied permission locally and stops repeatedly prompting the browser after permission is blocked.

Homepage geolocation opt-in fix (April 23, 2026):
- Stopped the homepage prayer widget from automatically requesting browser geolocation on page load.
- Added an explicit “Use my location” button so Chrome no longer logs repeated geolocation permission warnings unless the visitor chooses to enable local timings.
- Fallback prayer times and Qibla guidance remain visible by default.

Stable Bengali and Arabic page translation fix (April 24, 2026):
- Added a bounded page translator that fills in hardcoded English text after Bengali or Arabic is selected, then stops instead of continuously mutating the page.
- Restores original text only when the visitor changes language, preventing the visible Bengali/English shaking loop.
- Added client-side translation caching for the current session and Arabic support in the translation API.
