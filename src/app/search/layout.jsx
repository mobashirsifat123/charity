export const metadata = {
  title: "Search",
  description: "Internal search across articles and fatwas on IRWAA.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SearchLayout({ children }) {
  return children;
}
