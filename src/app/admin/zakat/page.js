import AdminSectionPlaceholder from '@/components/admin/AdminSectionPlaceholder';

export default function AdminZakatPage() {
  return (
    <AdminSectionPlaceholder
      eyebrow="Charity"
      title="Zakat Management"
      description="This workspace will centralize zakat-specific settings, reporting, and payment transparency so the charity team can separate zakat from general giving."
    />
  );
}
