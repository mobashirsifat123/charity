"use client";

const ColorPalate = () => {
    // A placeholder for a theme/color switcher, 
    // keeping it empty for now to fix build errors without adding unnecessary DOM complexity
    return null;
};

export default ColorPalate;
