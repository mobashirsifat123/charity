"use client";

const CustomCursor = () => {
    // A simplified custom cursor placeholder that doesn't track mouse to avoid performance overhead in this fix
    return null;
};

export default CustomCursor;
